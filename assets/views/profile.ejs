<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;1,100;1,400&display=swap" rel="stylesheet">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <header class="dashboardHeader">
        <div class="headerLogo">
            <img src="../img/RC logo blue.png" alt="RoommateConnect Logo">
        </div>
        <div class="rcSlogan">
            <p>Unlock the perfect roommate!</p>
        </div>
    </header>
    <main class="dashboardContainer">
        <div class="dashboardMenu">
            <div class="userImgContainer">
                <img src="../img/defaultUserImg.png" class="profileImg" alt="User's profile picture">
            </div>
            <div class="accountBtn">
                <a href="/account" class="btn btn-primary">Account</a>
            </div>
            <div class="logoutBtn">
                <a href="/logout" class="btn btn-outline-warning">Logout</a>
            </div>
        </div>
        <div class="dashboardHero">
            <section class="userDataHero">
                <h2>Welcome Back <%= user.first_name %>! </h2>
                <h4>Your data:</h4>
                <p>Email: <%= user.email %></p>
                <p>Birthdate: <%= new Date(user.birthdate).toLocaleDateString() %></p>
                <p>Roomie preference: <%= user.roommate_preference %></p>
            </section>
            <section class="roommatesDataSection">
                <h3>Look for your roommate</h3>
                <table class="roommatesDataTable">
                    <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Roommate Preference</th>
                    <th>More Info</th>
                    </tr>
                    <% people.forEach(row => { %>
                    <tr>
                        <td><%= row.first_name %></td>
                        <td><%= row.last_name %></td>
                        <td><%= row.email %></td>
                        <td><%= row.roommate_preference %></td>
                        <td><%= row.additional_info %></td>
                    </tr>
                    <% }); %>
                </table>
            </section>
        </div>
    </main>
</body>
</html>